{
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "watch": "run-when-changed --watch 'src/**' --exec 'yarn run start'"
  },
  "devDependencies": {
    "run-when-changed": "^2.1.0"
  }
}
